{
  "name": "rqe",
  "version": "0.2.0",
  "description": "",
  "main": "dist/index.js",
  "types":"dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "prepare": "tsc -p .",
    "generate-docs": "mkdir -p dist/api-extractor && api-extractor run --local && api-documenter markdown -i temp -o docs/api && mv docs/api/* docs"
  },
  "author": "andyfischer",
  "license": "Unlicense",
  "devDependencies": {
    "@types/node": "^16.11.12",
    "typescript": "^4.5.2"
  }
}
